import { StyleSheet } from "react-native";

const LayoutStyles = StyleSheet.create({
  settingListScrollView: { marginHorizontal: 12, gap: 2, marginTop: 32, paddingBottom: 32 },
});

export default LayoutStyles;
